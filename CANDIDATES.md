
## Обновление 2026-08-22 (пост-рефакторинг)

**ОПРОВЕРЖЕНО: solitary-client теория.** Блинк воспроизводится БЕЗ
фуллскрин-окон ⇒ быстрый путь renderMonitor не единственный источник.
Прямой сканаут (dso) остаётся под подозрением — теперь виден в трейсе.

**Логгер введён** (renderStage, debug_logs=1 → /tmp/gloview.log): по строке
на кадр анимации — t=+ms, open, prog, kk (альфа бэкдропа), frost (1-e),
blurOK (кэш блюра жив), bdDrawn, soli, dso, dmgE (пустой damage снапшот).
Маркеры "=== OPEN ===" / "=== CLOSE ... ===". Сопоставлять с PNG-кадрами
wf-recorder по номерам строк между маркерами.

**Bug B, математика покрытия:** kk + frost ≥ 1.0 всё закрытие ⇒ «нехватки
блюра» нет; рабочая гипотеза — подмена КОНТЕНТА (кэш обоев vs живой
blur-behind реальных окон). Логгер это подтвердит/опровергнет числами.

## Обновление 2 (post claude_review.md)

- **Solitary/DS вычеркнуты окончательно** для нашего repro: оба пути жёстко
  завязаны на Fullscreen::getFullscreenWindow(); без фуллскрина не включаются.
  (Подтвердить dso/soli=0 в PRE-probe по всем сессиям.)
- **Новый главный кандидат: CDamageRing.** Ring глубины 3; rotate() съедает
  m_current на КАЖДОМ beginRender. Один damage() в open() покрывает только
  первый потребивший его swapchain-image; остальные 1-2 образа могут
  презентоваться stale (вплоть до never-composited) — ровно «1-3 чёрных
  кадра». Фикс: `m->m_forceFullFrames = 3` в open() (ea8891e, патч внешнего
  ревью; цитаты сверены с pinned). Ждёт покадрового подтверждения.
- **render:new_render_scheduling = TRUE на этой машине** (Клод считал дефолт
  false и вычеркнул пункт 4 — здесь он ОТКРЫТ): асинхронный explicit-sync
  commit-путь активен. Дешёвый эксперимент при сохранении блинка после
  ea8891e: `hyprctl eval 'hl.config({ render = { new_render_scheduling = 0 } })'`
  → повторить тогглы.
- DS-leave без forceFullFrames — реальный гэп Hyprland (апстрим-репорт,
  вне нашего репо).

## Bug B — закрыт
Механизм фикса: windowBlurEligible (копия shouldBlur) + собственный preBlur-
цикл Hyprland. Правило: никогда не перезаписывать мониторный m_blurFB из
плагина — проекция/содержимое не совпадут с родным конвейером (A/B: d33e239).

## Обновление 2026-09-05 (Bug A продолжение, после c021c11/e3c391d)

Файл был неактуален с момента "Bug B закрыт" — Bug A (чёрный блинк) с тех пор
получил opaque-base фикс (c021c11) и diag-логирование (e3c391d), см. отдельную
сводку сессии. Ключевое НОВОЕ наблюдение той сводки, которого не было здесь:
блинк специфично показывает окно в РЕАЛЬНОМ полном размере/позиции (не hidden
вообще), а не просто "неправильный бэкдроп под корректно скрытым окном" — то
есть весь PainterPass ещё не докомпозирован в эти кадры, не только backdrop.

**Важно, что предыдущая сессия (эта сводка) не учла:** пункт выше в этом же
файле — `render:new_render_scheduling = TRUE на этой машине` — асинхронный
explicit-sync commit-путь. Это ДЕШЕВЛЕ всех вариантов из "куда копать дальше"
новой сводки: runtime-тумблер без ребилда,
`hyprctl eval 'hl.config({ render = { new_render_scheduling = 0 } })'`,
повторить тогглы. Если блинк пропадает/меняется — это прямое попадание в
асинхронный commit-путь как источник задержки CPU-решение → скан-аут, а не
что-то специфичное для gloview. ПРОВЕРИТЬ ЭТО ПЕРВЫМ в следующей сессии —
дешевле любого code change.

**Код перепроверен (чтение исходников, без билда — нет живого Hyprland/GPU в
этой среде):**
- `open()` — одна синхронная call-stack без await/yield; `m_active=true` и
  `m_openStamp` ставятся в одной строке друг за другом. Диагностика
  `entryToActiveUs` добавлена, но по чтению кода ожидается ~0 — маловероятный
  источник многокадровой задержки.
- `hl.plugin.gloview.toggle` (addLuaFunction) вызывает `toggle()` СИНХРОННО,
  без промежуточного IPC/сокета — это НЕ то же самое, что `exec, hyprctl
  gloview` (тот путь реально форкает процесс и идёт через сокет, потенциально
  многокадровая задержка). Если бинд в конфиге пользователя использует
  `hl.bind(..., hl.plugin.gloview.toggle)` (README-паттерн), Lua-диспетчеризация
  сама по себе — не подозреваемый.
- `shouldRenderWindow` оценивается РАНО в кадре (см. комментарий у
  m_pendingDeactivate, session.cpp) — задолго до RENDER_LAST_MOMENT, где
  добавляется PainterPass. На бумаге, раз `m_active` синхронно true до начала
  render() для этого тика, окно должно скрыться с первого же кадра. Реальная
  задержка (если не render_scheduling) — с высокой вероятностью на стороне
  Hyprland: буферизация представления (double/triple buffering) между
  "CPU решил" и "GPU реально показал", то есть НЕ багом gloview как такового.

**Новая инструментация в этом коммите** (чисто диагностическая, поведение не
меняет кроме forceFullFrames 3→8, см. ниже):
- `Overview::frameTick()` — монотонный счётчик RENDER_LAST_MOMENT (публичный,
  тикает независимо от m_active) — общая система координат для всех логов.
- `TOGGLE tick=…` / `OPEN tick=… entryToActiveUs=…` — session.cpp.
- `SRW tick=… win=… class=… active=… hide=…` в hkShouldRenderWindow — только
  пока `opening()` (окно входной анимации), чтобы не заспамить лог. САМОЕ
  прямое свидетельство: покажет, действительно ли хук вызывается с
  active=true уже на первом тике после OPEN, или есть реальный гэп.
- `forceFF=` добавлено в существующий `F t=+…` трейс.
- ЭКСПЕРИМЕНТ (не подтверждённый фикс): `m_forceFullFrames` 3→8 — см. комментарий
  в session.cpp прямо на этой строке для обоснования и критерия успеха.

## Обновление 2026-09-05 (сессия 2, шаг 1: render_scheduling тест)

**Результат: render:new_render_scheduling НЕ влияет на блинк.**
- Исходное значение на машине: `false` (set=true, т.е. явно выставлено ранее).
- Включили `true` (как было по CANDIDATES.md), 5-6 тогглов — блинк БЕЗ ИЗМЕНЕНИЙ.
- Вернули `false`. Рендер-скедулинг — НЕ причина блинка.

## Обновление 2026-09-05 (сессия 2, шаг 4-5: покадровый лог + развилка)

**Результат лог-анализа (7be730f, commit tick=527):**
- `TOGGLE` и `OPEN` — один кадр (tick=527), задержки диспетчеризации нет.
- `entryToActiveUs=1764µs` (~1.7мс) — fast, не источник.
- `SRW hide=0` на ВСЕХ тиках (527-547) — НО это **ожидаемо**: терминалы `foot`
  не на активном воркспейсе и не в `m_tiles`, `shouldHideWindow` корректно
  их не скрывает (строка 757: `w->m_workspace == aw`). Хук работает правильно.
- `forceFF=7→0` за 8 кадров (tick 528-535) — 8 принудительных full-composite
  кадров. На tick 535 forceFF=0, а prog=0.545 (середина анимации).

**Итог по gloview-стороне:** хук shouldRenderWindow, open(), диспетчеризация
бинда — всё чисто, CPU-side гэпа нет. Блинк — presentation latency на стороне
Hyprland/DRM: 8 кадров forceFullFrames доходят до экрана с задержкой
double/triple buffering. gloview не может это исправить.

**Варианты действий:**
1. invasive screen-capture bridge (пункт 4 из исходной сводки) — единственный
   надёжный путь, захватывает реальный кадр и покажет где именно задержка.
2. Апстрим-репорт в Hyprland с цифрами (8 кадров, ~120мс до forceFF=0).
3. Жить с блинком — косметический дефект, ~120мс.
